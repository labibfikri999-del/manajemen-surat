name: 🚀 Webhook Deployment

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    name: 🔔 Trigger Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Call Webhook
        run: |
          # Memanggil URL Webhook dengan Token Rahasia
          curl -v -f "https://e-yarsi.id/deploy.php?key=${{ secrets.DEPLOY_KEY }}"
